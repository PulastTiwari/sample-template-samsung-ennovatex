# Example environment variables for Sentinel-QoS
# Copy to .env or set in your hosting environment

# Backend admin credentials (change for production)
SENTINEL_ADMIN_USER=admin
SENTINEL_ADMIN_PASS=admin

# LLM toggle and model selection
SENTINEL_LLM_ENABLED=true
SENTINEL_LLM_MODEL=mistral

# Model path (when not using default locations)
MODEL_PATH=src/data/archive/sentry_model.pkl

# Frontend API URL used by Next.js
NEXT_PUBLIC_API_URL=http://localhost:8000
